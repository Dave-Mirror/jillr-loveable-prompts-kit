
import React from 'react';
import UnifiedMapView from '@/components/unified-map/UnifiedMapView';
import PageContainer from '@/components/navigation/PageContainer';

const UnifiedMap = () => {
  return (
    <PageContainer>
      <UnifiedMapView />
    </PageContainer>
  );
};

export default UnifiedMap;
